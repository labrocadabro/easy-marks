{
	"name": "heroku-test",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"heroku-postbuild": "cd frontend && npm install && npm run build"
	},
	"cacheDirectories": [
		"frontend/node_modules"
	],
	"dependencies": {
		"flowbite": "^2.3.0",
		"flowbite-react": "^0.7.2"
	}
}
